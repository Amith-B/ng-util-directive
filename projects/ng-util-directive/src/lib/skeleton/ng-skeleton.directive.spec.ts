import { NgSkeletonDirective } from './ng-skeleton.directive';

describe('NgSkeletonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSkeletonDirective();
    expect(directive).toBeTruthy();
  });
});
